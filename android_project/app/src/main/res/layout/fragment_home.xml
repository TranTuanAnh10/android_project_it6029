<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Scroll View chứa toàn bộ nội dung chính của màn hình -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="80dp"> <!-- Thêm padding để FAB không che mất nội dung cuối -->

            <!-- Header -->
            <!-- Giả định layout_home_header đã được định nghĩa -->
            <include layout="@layout/layout_home_header" />

            <!-- Nội dung cuộn -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="16dp">

                <!-- Giả định các include layouts đã được định nghĩa -->
                <include layout="@layout/layout_browser_cuisines" />

                <include layout="@layout/layout_top_picks" />

                <include layout="@layout/layout_best_selling" />

                <include layout="@layout/layout_ready_for_lunch" />
            </LinearLayout>
        </LinearLayout>
    </ScrollView>

    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/count_card"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="24dp">

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab_cart"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/label_basket_orders"
            app:backgroundTint="?attr/colorError"
            app:fabSize="auto"
            app:maxImageSize="30dp"
            app:srcCompat="@drawable/ic_order"
            app:tint="@android:color/white" />

        <!-- Badge số lượng -->
        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/tv_cart_count_badge"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_gravity="top|end"
            android:layout_marginTop="0dp"
            android:layout_marginEnd="0dp"
            android:background="@drawable/badge_background"
            android:elevation="12dp"
            android:gravity="center"
            android:paddingHorizontal="1dp"
            android:textColor="@android:color/holo_red_dark"
            android:textSize="10sp"
            tools:visibility="visible" />
    </FrameLayout>

    <!-- Overlay loading -->
    <FrameLayout
        android:id="@+id/loadingOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#CC000000"
        android:gravity="center"
        android:visibility="gone">

        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:indeterminate="true"
            android:indeterminateTint="@android:color/white" />
    </FrameLayout>

</FrameLayout>